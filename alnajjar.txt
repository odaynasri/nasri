•	Fetching & Pulling:
o	Remote Branches: git branch -r to view the remote branches our local repository knows about. In other meaning when we clone a repo from Github, the origin branch that the last commit stand of. And it could be also more than one (Remote Tracking Branch).
o	Sometimes we clone a repo from Github and therefor will be more or multi branches. BUT when we cloning the repo Git will only show me the remote branch where exactly the Github repo was, when I cloned it, I will not see the other branches with git branch BUT I will see the other branches with git branch -r and it will show me the origin/<branch-name> and to be able to get the other branches on the local machine, I simply use the command git switch <branch.name> and therefore git will automatically see connect the name of local machine branch with the Github remote branch with the same name.
o	Fetching: Fetching allows us to download the changes from the remota repository, BUT those changes will not be automatically integrated into our working files. It lets us see what others have been working on, without having to merge those changes into the local repo. git fetch <remote> command fetches branches and history from 
